name: release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  binaries:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - x86_64-unknown-linux-gnu
          - x86_64-unknown-linux-musl
          - aarch64-unknown-linux-gnu
          - aarch64-unknown-linux-musl
          - arm-unknown-linux-gnueabihf
          - arm-unknown-linux-musleabihf
          - armv7-unknown-linux-gnueabihf
          - armv7-unknown-linux-musleabihf
          # mips (tier 3, built from source via build-std)
          - mips-unknown-linux-gnu
          - mipsel-unknown-linux-gnu
          - mips64-unknown-linux-gnuabi64
          - mips64el-unknown-linux-gnuabi64
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust nightly
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rust-src

      - name: Install cross
        uses: taiki-e/install-action@v2
        with:
          tool: cross

      - name: Build
        run: |
          flags=""
          case "${{ matrix.target }}" in
            mips*) flags="-Zbuild-std=std,panic_abort" ;;  # tier 3: build std from source
          esac
          cross build --release --locked $flags --target ${{ matrix.target }}

      - name: Package
        run: |
          install -D "target/${{ matrix.target }}/release/zeronat" "out/zeronat"
          tar -C out -czf "zeronat-${{ matrix.target }}.tar.gz" zeronat
          sha256sum "zeronat-${{ matrix.target }}.tar.gz" > "zeronat-${{ matrix.target }}.tar.gz.sha256"

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: bin-${{ matrix.target }}
          path: out/zeronat
          if-no-files-found: error

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            zeronat-${{ matrix.target }}.tar.gz
            zeronat-${{ matrix.target }}.tar.gz.sha256

  docker:
    needs: binaries
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Docker platform -> static musl target. scratch images COPY a prebuilt
      # binary, so no emulation is needed to assemble them.
      - uses: actions/download-artifact@v4
        with: { name: bin-x86_64-unknown-linux-musl, path: dist/linux/amd64 }
      - uses: actions/download-artifact@v4
        with: { name: bin-aarch64-unknown-linux-musl, path: dist/linux/arm64 }
      - uses: actions/download-artifact@v4
        with: { name: bin-armv7-unknown-linux-musleabihf, path: dist/linux/arm/v7 }
      - uses: actions/download-artifact@v4
        with: { name: bin-arm-unknown-linux-musleabihf, path: dist/linux/arm/v6 }

      - name: Mark binaries executable
        run: find dist -name zeronat -exec chmod +x {} +

      - uses: docker/setup-buildx-action@v3

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/zeronat
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=ref,event=branch
            type=raw,value=edge,enable=${{ github.event_name == 'workflow_dispatch' }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64,linux/arm64,linux/arm/v7,linux/arm/v6
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
