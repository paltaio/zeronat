services:
  zeronat:
    image: ${ZERONAT_IMAGE:-ghcr.io/paltaio/zeronat:protocol-v6}
    container_name: zeronat
    network_mode: host
    restart: unless-stopped
    env_file: .env
    volumes:
      - /etc/zeronat/data:/etc/zeronat/data
    command: ${ZERONAT_ARGS}
